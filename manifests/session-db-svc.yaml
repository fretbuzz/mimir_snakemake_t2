---
apiVersion: v1
kind: Service
metadata:
  name: session-db
  labels:
    name: session-db
    app: session-db
  namespace: default
spec:
  ports:
    # the port that this service should serve on
  - port: 6379
    targetPort: 6379
    name: redis
  selector:
    name: session-db
