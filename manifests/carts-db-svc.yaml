---
apiVersion: v1
kind: Service
metadata:
  name: carts-db
  labels:
    name: carts-db
    app: carts-db
  namespace: default
spec:
  ports:
    # the port that this service should serve on
  - port: 27017
    targetPort: 27017
    name: mongo
  selector:
    name: carts-db
