%autogenerated
hacl(internet, _, _, _).
attackGoal(execCode(internet, _)).
vulExists(internet, 'attacker_can_access_anything', is_computer).
networkServiceInfo(internet, is_computer, _, _, _).
vulProperty('attacker_can_access_anything', remoteExploit, privEscalation).
vulProperty('is_a_vip', remoteExploit, privEscalation).

hacl(cart_pod,carts_db_vip, TCP, 27017).
hacl(cart_vip,carts_db_pod, TCP, 27017).
hacl(carts_db_pod,cart_vip, TCP, 27017).
hacl(carts_db_pod,cart_pod, TCP, 27017).
hacl(catalogue_db_pod,carts_db_vip, TCP, 27017).
hacl(catalogue_db_vip,carts_db_pod, TCP, 27017).
hacl(carts_db_pod,catalogue_db_vip, TCP, 27017).
hacl(carts_db_pod,catalogue_db_pod, TCP, 27017).
hacl(catalogue_pod,carts_db_vip, TCP, 27017).
hacl(catalogue_vip,carts_db_pod, TCP, 27017).
hacl(carts_db_pod,catalogue_vip, TCP, 27017).
hacl(carts_db_pod,catalogue_pod, TCP, 27017).
hacl(front_end_pod,carts_db_vip, TCP, 27017).
hacl(front_end_vip,carts_db_pod, TCP, 27017).
hacl(carts_db_pod,front_end_vip, TCP, 27017).
hacl(carts_db_pod,front_end_pod, TCP, 27017).
hacl(orders_db_pod,carts_db_vip, TCP, 27017).
hacl(orders_db_vip,carts_db_pod, TCP, 27017).
hacl(carts_db_pod,orders_db_vip, TCP, 27017).
hacl(carts_db_pod,orders_db_pod, TCP, 27017).
hacl(orders_pod,carts_db_vip, TCP, 27017).
hacl(orders_vip,carts_db_pod, TCP, 27017).
hacl(carts_db_pod,orders_vip, TCP, 27017).
hacl(carts_db_pod,orders_pod, TCP, 27017).
hacl(payment_pod,carts_db_vip, TCP, 27017).
hacl(payment_vip,carts_db_pod, TCP, 27017).
hacl(carts_db_pod,payment_vip, TCP, 27017).
hacl(carts_db_pod,payment_pod, TCP, 27017).
hacl(queue_master_pod,carts_db_vip, TCP, 27017).
hacl(queue_master_vip,carts_db_pod, TCP, 27017).
hacl(carts_db_pod,queue_master_vip, TCP, 27017).
hacl(carts_db_pod,queue_master_pod, TCP, 27017).
hacl(rabbitmq_pod,carts_db_vip, TCP, 27017).
hacl(rabbitmq_vip,carts_db_pod, TCP, 27017).
hacl(carts_db_pod,rabbitmq_vip, TCP, 27017).
hacl(carts_db_pod,rabbitmq_pod, TCP, 27017).
hacl(shipping_pod,carts_db_vip, TCP, 27017).
hacl(shipping_vip,carts_db_pod, TCP, 27017).
hacl(carts_db_pod,shipping_vip, TCP, 27017).
hacl(carts_db_pod,shipping_pod, TCP, 27017).
hacl(user_db_pod,carts_db_vip, TCP, 27017).
hacl(user_db_vip,carts_db_pod, TCP, 27017).
hacl(carts_db_pod,user_db_vip, TCP, 27017).
hacl(carts_db_pod,user_db_pod, TCP, 27017).
hacl(user_pod,carts_db_vip, TCP, 27017).
hacl(user_vip,carts_db_pod, TCP, 27017).
hacl(carts_db_pod,user_vip, TCP, 27017).
hacl(carts_db_pod,user_pod, TCP, 27017).
hacl(carts_db_pod,kube_dns_vip, TCP, 27017).
hacl(internet,carts_db_vip, TCP, 27017).
hacl(internet,carts_db_pod, TCP, 27017).
hacl(carts_db_pod,internet, TCP, 27017).
hacl(carts_db_pod,internet, TCP, 27017).
hacl(carts_db_pod,carts_db_vip, TCP, 27017).
hacl(carts_db_vip,carts_db_pod, _, _).
vulExists(carts_db_pod, 'attacker_can_access_anything', is_computer).
networkServiceInfo(carts_db_pod, is_computer,  _, _ , _).
vulExists(carts_db_vip, 'is_a_vip', is_vip).
networkServiceInfo(carts_db_vip, is_vip, TCP, 27017, _).

hacl(carts_db_pod,cart_vip, TCP, 80).
hacl(carts_db_vip,cart_pod, TCP, 80).
hacl(cart_pod,carts_db_vip, TCP, 80).
hacl(cart_pod,carts_db_pod, TCP, 80).
hacl(catalogue_db_pod,cart_vip, TCP, 80).
hacl(catalogue_db_vip,cart_pod, TCP, 80).
hacl(cart_pod,catalogue_db_vip, TCP, 80).
hacl(cart_pod,catalogue_db_pod, TCP, 80).
hacl(catalogue_pod,cart_vip, TCP, 80).
hacl(catalogue_vip,cart_pod, TCP, 80).
hacl(cart_pod,catalogue_vip, TCP, 80).
hacl(cart_pod,catalogue_pod, TCP, 80).
hacl(front_end_pod,cart_vip, TCP, 80).
hacl(front_end_vip,cart_pod, TCP, 80).
hacl(cart_pod,front_end_vip, TCP, 80).
hacl(cart_pod,front_end_pod, TCP, 80).
hacl(orders_db_pod,cart_vip, TCP, 80).
hacl(orders_db_vip,cart_pod, TCP, 80).
hacl(cart_pod,orders_db_vip, TCP, 80).
hacl(cart_pod,orders_db_pod, TCP, 80).
hacl(orders_pod,cart_vip, TCP, 80).
hacl(orders_vip,cart_pod, TCP, 80).
hacl(cart_pod,orders_vip, TCP, 80).
hacl(cart_pod,orders_pod, TCP, 80).
hacl(payment_pod,cart_vip, TCP, 80).
hacl(payment_vip,cart_pod, TCP, 80).
hacl(cart_pod,payment_vip, TCP, 80).
hacl(cart_pod,payment_pod, TCP, 80).
hacl(queue_master_pod,cart_vip, TCP, 80).
hacl(queue_master_vip,cart_pod, TCP, 80).
hacl(cart_pod,queue_master_vip, TCP, 80).
hacl(cart_pod,queue_master_pod, TCP, 80).
hacl(rabbitmq_pod,cart_vip, TCP, 80).
hacl(rabbitmq_vip,cart_pod, TCP, 80).
hacl(cart_pod,rabbitmq_vip, TCP, 80).
hacl(cart_pod,rabbitmq_pod, TCP, 80).
hacl(shipping_pod,cart_vip, TCP, 80).
hacl(shipping_vip,cart_pod, TCP, 80).
hacl(cart_pod,shipping_vip, TCP, 80).
hacl(cart_pod,shipping_pod, TCP, 80).
hacl(user_db_pod,cart_vip, TCP, 80).
hacl(user_db_vip,cart_pod, TCP, 80).
hacl(cart_pod,user_db_vip, TCP, 80).
hacl(cart_pod,user_db_pod, TCP, 80).
hacl(user_pod,cart_vip, TCP, 80).
hacl(user_vip,cart_pod, TCP, 80).
hacl(cart_pod,user_vip, TCP, 80).
hacl(cart_pod,user_pod, TCP, 80).
hacl(cart_pod,kube_dns_vip, TCP, 80).
hacl(internet,cart_vip, TCP, 80).
hacl(internet,cart_pod, TCP, 80).
hacl(cart_pod,internet, TCP, 80).
hacl(cart_pod,internet, TCP, 80).
hacl(cart_pod,cart_vip, TCP, 80).
hacl(cart_vip,cart_pod, _, _).
vulExists(cart_pod, 'attacker_can_access_anything', is_computer).
networkServiceInfo(cart_pod, is_computer,  _, _ , _).
vulExists(cart_vip, 'is_a_vip', is_vip).
networkServiceInfo(cart_vip, is_vip, TCP, 80, _).

hacl(carts_db_pod,catalogue_db_vip, TCP, 3306).
hacl(carts_db_vip,catalogue_db_pod, TCP, 3306).
hacl(catalogue_db_pod,carts_db_vip, TCP, 3306).
hacl(catalogue_db_pod,carts_db_pod, TCP, 3306).
hacl(cart_pod,catalogue_db_vip, TCP, 3306).
hacl(cart_vip,catalogue_db_pod, TCP, 3306).
hacl(catalogue_db_pod,cart_vip, TCP, 3306).
hacl(catalogue_db_pod,cart_pod, TCP, 3306).
hacl(catalogue_pod,catalogue_db_vip, TCP, 3306).
hacl(catalogue_vip,catalogue_db_pod, TCP, 3306).
hacl(catalogue_db_pod,catalogue_vip, TCP, 3306).
hacl(catalogue_db_pod,catalogue_pod, TCP, 3306).
hacl(front_end_pod,catalogue_db_vip, TCP, 3306).
hacl(front_end_vip,catalogue_db_pod, TCP, 3306).
hacl(catalogue_db_pod,front_end_vip, TCP, 3306).
hacl(catalogue_db_pod,front_end_pod, TCP, 3306).
hacl(orders_db_pod,catalogue_db_vip, TCP, 3306).
hacl(orders_db_vip,catalogue_db_pod, TCP, 3306).
hacl(catalogue_db_pod,orders_db_vip, TCP, 3306).
hacl(catalogue_db_pod,orders_db_pod, TCP, 3306).
hacl(orders_pod,catalogue_db_vip, TCP, 3306).
hacl(orders_vip,catalogue_db_pod, TCP, 3306).
hacl(catalogue_db_pod,orders_vip, TCP, 3306).
hacl(catalogue_db_pod,orders_pod, TCP, 3306).
hacl(payment_pod,catalogue_db_vip, TCP, 3306).
hacl(payment_vip,catalogue_db_pod, TCP, 3306).
hacl(catalogue_db_pod,payment_vip, TCP, 3306).
hacl(catalogue_db_pod,payment_pod, TCP, 3306).
hacl(queue_master_pod,catalogue_db_vip, TCP, 3306).
hacl(queue_master_vip,catalogue_db_pod, TCP, 3306).
hacl(catalogue_db_pod,queue_master_vip, TCP, 3306).
hacl(catalogue_db_pod,queue_master_pod, TCP, 3306).
hacl(rabbitmq_pod,catalogue_db_vip, TCP, 3306).
hacl(rabbitmq_vip,catalogue_db_pod, TCP, 3306).
hacl(catalogue_db_pod,rabbitmq_vip, TCP, 3306).
hacl(catalogue_db_pod,rabbitmq_pod, TCP, 3306).
hacl(shipping_pod,catalogue_db_vip, TCP, 3306).
hacl(shipping_vip,catalogue_db_pod, TCP, 3306).
hacl(catalogue_db_pod,shipping_vip, TCP, 3306).
hacl(catalogue_db_pod,shipping_pod, TCP, 3306).
hacl(user_db_pod,catalogue_db_vip, TCP, 3306).
hacl(user_db_vip,catalogue_db_pod, TCP, 3306).
hacl(catalogue_db_pod,user_db_vip, TCP, 3306).
hacl(catalogue_db_pod,user_db_pod, TCP, 3306).
hacl(user_pod,catalogue_db_vip, TCP, 3306).
hacl(user_vip,catalogue_db_pod, TCP, 3306).
hacl(catalogue_db_pod,user_vip, TCP, 3306).
hacl(catalogue_db_pod,user_pod, TCP, 3306).
hacl(catalogue_db_pod,kube_dns_vip, TCP, 3306).
hacl(internet,catalogue_db_vip, TCP, 3306).
hacl(internet,catalogue_db_pod, TCP, 3306).
hacl(catalogue_db_pod,internet, TCP, 3306).
hacl(catalogue_db_pod,internet, TCP, 3306).
hacl(catalogue_db_pod,catalogue_db_vip, TCP, 3306).
hacl(catalogue_db_vip,catalogue_db_pod, _, _).
vulExists(catalogue_db_pod, 'attacker_can_access_anything', is_computer).
networkServiceInfo(catalogue_db_pod, is_computer,  _, _ , _).
vulExists(catalogue_db_vip, 'is_a_vip', is_vip).
networkServiceInfo(catalogue_db_vip, is_vip, TCP, 3306, _).

hacl(carts_db_pod,catalogue_vip, TCP, 80).
hacl(carts_db_vip,catalogue_pod, TCP, 80).
hacl(catalogue_pod,carts_db_vip, TCP, 80).
hacl(catalogue_pod,carts_db_pod, TCP, 80).
hacl(cart_pod,catalogue_vip, TCP, 80).
hacl(cart_vip,catalogue_pod, TCP, 80).
hacl(catalogue_pod,cart_vip, TCP, 80).
hacl(catalogue_pod,cart_pod, TCP, 80).
hacl(catalogue_db_pod,catalogue_vip, TCP, 80).
hacl(catalogue_db_vip,catalogue_pod, TCP, 80).
hacl(catalogue_pod,catalogue_db_vip, TCP, 80).
hacl(catalogue_pod,catalogue_db_pod, TCP, 80).
hacl(front_end_pod,catalogue_vip, TCP, 80).
hacl(front_end_vip,catalogue_pod, TCP, 80).
hacl(catalogue_pod,front_end_vip, TCP, 80).
hacl(catalogue_pod,front_end_pod, TCP, 80).
hacl(orders_db_pod,catalogue_vip, TCP, 80).
hacl(orders_db_vip,catalogue_pod, TCP, 80).
hacl(catalogue_pod,orders_db_vip, TCP, 80).
hacl(catalogue_pod,orders_db_pod, TCP, 80).
hacl(orders_pod,catalogue_vip, TCP, 80).
hacl(orders_vip,catalogue_pod, TCP, 80).
hacl(catalogue_pod,orders_vip, TCP, 80).
hacl(catalogue_pod,orders_pod, TCP, 80).
hacl(payment_pod,catalogue_vip, TCP, 80).
hacl(payment_vip,catalogue_pod, TCP, 80).
hacl(catalogue_pod,payment_vip, TCP, 80).
hacl(catalogue_pod,payment_pod, TCP, 80).
hacl(queue_master_pod,catalogue_vip, TCP, 80).
hacl(queue_master_vip,catalogue_pod, TCP, 80).
hacl(catalogue_pod,queue_master_vip, TCP, 80).
hacl(catalogue_pod,queue_master_pod, TCP, 80).
hacl(rabbitmq_pod,catalogue_vip, TCP, 80).
hacl(rabbitmq_vip,catalogue_pod, TCP, 80).
hacl(catalogue_pod,rabbitmq_vip, TCP, 80).
hacl(catalogue_pod,rabbitmq_pod, TCP, 80).
hacl(shipping_pod,catalogue_vip, TCP, 80).
hacl(shipping_vip,catalogue_pod, TCP, 80).
hacl(catalogue_pod,shipping_vip, TCP, 80).
hacl(catalogue_pod,shipping_pod, TCP, 80).
hacl(user_db_pod,catalogue_vip, TCP, 80).
hacl(user_db_vip,catalogue_pod, TCP, 80).
hacl(catalogue_pod,user_db_vip, TCP, 80).
hacl(catalogue_pod,user_db_pod, TCP, 80).
hacl(user_pod,catalogue_vip, TCP, 80).
hacl(user_vip,catalogue_pod, TCP, 80).
hacl(catalogue_pod,user_vip, TCP, 80).
hacl(catalogue_pod,user_pod, TCP, 80).
hacl(catalogue_pod,kube_dns_vip, TCP, 80).
hacl(internet,catalogue_vip, TCP, 80).
hacl(internet,catalogue_pod, TCP, 80).
hacl(catalogue_pod,internet, TCP, 80).
hacl(catalogue_pod,internet, TCP, 80).
hacl(catalogue_pod,catalogue_vip, TCP, 80).
hacl(catalogue_vip,catalogue_pod, _, _).
vulExists(catalogue_pod, 'attacker_can_access_anything', is_computer).
networkServiceInfo(catalogue_pod, is_computer,  _, _ , _).
vulExists(catalogue_vip, 'is_a_vip', is_vip).
networkServiceInfo(catalogue_vip, is_vip, TCP, 80, _).

hacl(carts_db_pod,front_end_vip, TCP, 80).
hacl(carts_db_vip,front_end_pod, TCP, 80).
hacl(front_end_pod,carts_db_vip, TCP, 80).
hacl(front_end_pod,carts_db_pod, TCP, 80).
hacl(cart_pod,front_end_vip, TCP, 80).
hacl(cart_vip,front_end_pod, TCP, 80).
hacl(front_end_pod,cart_vip, TCP, 80).
hacl(front_end_pod,cart_pod, TCP, 80).
hacl(catalogue_db_pod,front_end_vip, TCP, 80).
hacl(catalogue_db_vip,front_end_pod, TCP, 80).
hacl(front_end_pod,catalogue_db_vip, TCP, 80).
hacl(front_end_pod,catalogue_db_pod, TCP, 80).
hacl(catalogue_pod,front_end_vip, TCP, 80).
hacl(catalogue_vip,front_end_pod, TCP, 80).
hacl(front_end_pod,catalogue_vip, TCP, 80).
hacl(front_end_pod,catalogue_pod, TCP, 80).
hacl(orders_db_pod,front_end_vip, TCP, 80).
hacl(orders_db_vip,front_end_pod, TCP, 80).
hacl(front_end_pod,orders_db_vip, TCP, 80).
hacl(front_end_pod,orders_db_pod, TCP, 80).
hacl(orders_pod,front_end_vip, TCP, 80).
hacl(orders_vip,front_end_pod, TCP, 80).
hacl(front_end_pod,orders_vip, TCP, 80).
hacl(front_end_pod,orders_pod, TCP, 80).
hacl(payment_pod,front_end_vip, TCP, 80).
hacl(payment_vip,front_end_pod, TCP, 80).
hacl(front_end_pod,payment_vip, TCP, 80).
hacl(front_end_pod,payment_pod, TCP, 80).
hacl(queue_master_pod,front_end_vip, TCP, 80).
hacl(queue_master_vip,front_end_pod, TCP, 80).
hacl(front_end_pod,queue_master_vip, TCP, 80).
hacl(front_end_pod,queue_master_pod, TCP, 80).
hacl(rabbitmq_pod,front_end_vip, TCP, 80).
hacl(rabbitmq_vip,front_end_pod, TCP, 80).
hacl(front_end_pod,rabbitmq_vip, TCP, 80).
hacl(front_end_pod,rabbitmq_pod, TCP, 80).
hacl(shipping_pod,front_end_vip, TCP, 80).
hacl(shipping_vip,front_end_pod, TCP, 80).
hacl(front_end_pod,shipping_vip, TCP, 80).
hacl(front_end_pod,shipping_pod, TCP, 80).
hacl(user_db_pod,front_end_vip, TCP, 80).
hacl(user_db_vip,front_end_pod, TCP, 80).
hacl(front_end_pod,user_db_vip, TCP, 80).
hacl(front_end_pod,user_db_pod, TCP, 80).
hacl(user_pod,front_end_vip, TCP, 80).
hacl(user_vip,front_end_pod, TCP, 80).
hacl(front_end_pod,user_vip, TCP, 80).
hacl(front_end_pod,user_pod, TCP, 80).
hacl(front_end_pod,kube_dns_vip, TCP, 80).
hacl(internet,front_end_vip, TCP, 80).
hacl(internet,front_end_pod, TCP, 80).
hacl(front_end_pod,internet, TCP, 80).
hacl(front_end_pod,internet, TCP, 80).
hacl(front_end_pod,front_end_vip, TCP, 80).
hacl(front_end_vip,front_end_pod, _, _).
vulExists(front_end_pod, 'attacker_can_access_anything', is_computer).
networkServiceInfo(front_end_pod, is_computer,  _, _ , _).
vulExists(front_end_vip, 'is_a_vip', is_vip).
networkServiceInfo(front_end_vip, is_vip, TCP, 80, _).

hacl(carts_db_pod,orders_db_vip, TCP, 27017).
hacl(carts_db_vip,orders_db_pod, TCP, 27017).
hacl(orders_db_pod,carts_db_vip, TCP, 27017).
hacl(orders_db_pod,carts_db_pod, TCP, 27017).
hacl(cart_pod,orders_db_vip, TCP, 27017).
hacl(cart_vip,orders_db_pod, TCP, 27017).
hacl(orders_db_pod,cart_vip, TCP, 27017).
hacl(orders_db_pod,cart_pod, TCP, 27017).
hacl(catalogue_db_pod,orders_db_vip, TCP, 27017).
hacl(catalogue_db_vip,orders_db_pod, TCP, 27017).
hacl(orders_db_pod,catalogue_db_vip, TCP, 27017).
hacl(orders_db_pod,catalogue_db_pod, TCP, 27017).
hacl(catalogue_pod,orders_db_vip, TCP, 27017).
hacl(catalogue_vip,orders_db_pod, TCP, 27017).
hacl(orders_db_pod,catalogue_vip, TCP, 27017).
hacl(orders_db_pod,catalogue_pod, TCP, 27017).
hacl(front_end_pod,orders_db_vip, TCP, 27017).
hacl(front_end_vip,orders_db_pod, TCP, 27017).
hacl(orders_db_pod,front_end_vip, TCP, 27017).
hacl(orders_db_pod,front_end_pod, TCP, 27017).
hacl(orders_pod,orders_db_vip, TCP, 27017).
hacl(orders_vip,orders_db_pod, TCP, 27017).
hacl(orders_db_pod,orders_vip, TCP, 27017).
hacl(orders_db_pod,orders_pod, TCP, 27017).
hacl(payment_pod,orders_db_vip, TCP, 27017).
hacl(payment_vip,orders_db_pod, TCP, 27017).
hacl(orders_db_pod,payment_vip, TCP, 27017).
hacl(orders_db_pod,payment_pod, TCP, 27017).
hacl(queue_master_pod,orders_db_vip, TCP, 27017).
hacl(queue_master_vip,orders_db_pod, TCP, 27017).
hacl(orders_db_pod,queue_master_vip, TCP, 27017).
hacl(orders_db_pod,queue_master_pod, TCP, 27017).
hacl(rabbitmq_pod,orders_db_vip, TCP, 27017).
hacl(rabbitmq_vip,orders_db_pod, TCP, 27017).
hacl(orders_db_pod,rabbitmq_vip, TCP, 27017).
hacl(orders_db_pod,rabbitmq_pod, TCP, 27017).
hacl(shipping_pod,orders_db_vip, TCP, 27017).
hacl(shipping_vip,orders_db_pod, TCP, 27017).
hacl(orders_db_pod,shipping_vip, TCP, 27017).
hacl(orders_db_pod,shipping_pod, TCP, 27017).
hacl(user_db_pod,orders_db_vip, TCP, 27017).
hacl(user_db_vip,orders_db_pod, TCP, 27017).
hacl(orders_db_pod,user_db_vip, TCP, 27017).
hacl(orders_db_pod,user_db_pod, TCP, 27017).
hacl(user_pod,orders_db_vip, TCP, 27017).
hacl(user_vip,orders_db_pod, TCP, 27017).
hacl(orders_db_pod,user_vip, TCP, 27017).
hacl(orders_db_pod,user_pod, TCP, 27017).
hacl(orders_db_pod,kube_dns_vip, TCP, 27017).
hacl(internet,orders_db_vip, TCP, 27017).
hacl(internet,orders_db_pod, TCP, 27017).
hacl(orders_db_pod,internet, TCP, 27017).
hacl(orders_db_pod,internet, TCP, 27017).
hacl(orders_db_pod,orders_db_vip, TCP, 27017).
hacl(orders_db_vip,orders_db_pod, _, _).
vulExists(orders_db_pod, 'attacker_can_access_anything', is_computer).
networkServiceInfo(orders_db_pod, is_computer,  _, _ , _).
vulExists(orders_db_vip, 'is_a_vip', is_vip).
networkServiceInfo(orders_db_vip, is_vip, TCP, 27017, _).

hacl(carts_db_pod,orders_vip, TCP, 80).
hacl(carts_db_vip,orders_pod, TCP, 80).
hacl(orders_pod,carts_db_vip, TCP, 80).
hacl(orders_pod,carts_db_pod, TCP, 80).
hacl(cart_pod,orders_vip, TCP, 80).
hacl(cart_vip,orders_pod, TCP, 80).
hacl(orders_pod,cart_vip, TCP, 80).
hacl(orders_pod,cart_pod, TCP, 80).
hacl(catalogue_db_pod,orders_vip, TCP, 80).
hacl(catalogue_db_vip,orders_pod, TCP, 80).
hacl(orders_pod,catalogue_db_vip, TCP, 80).
hacl(orders_pod,catalogue_db_pod, TCP, 80).
hacl(catalogue_pod,orders_vip, TCP, 80).
hacl(catalogue_vip,orders_pod, TCP, 80).
hacl(orders_pod,catalogue_vip, TCP, 80).
hacl(orders_pod,catalogue_pod, TCP, 80).
hacl(front_end_pod,orders_vip, TCP, 80).
hacl(front_end_vip,orders_pod, TCP, 80).
hacl(orders_pod,front_end_vip, TCP, 80).
hacl(orders_pod,front_end_pod, TCP, 80).
hacl(orders_db_pod,orders_vip, TCP, 80).
hacl(orders_db_vip,orders_pod, TCP, 80).
hacl(orders_pod,orders_db_vip, TCP, 80).
hacl(orders_pod,orders_db_pod, TCP, 80).
hacl(payment_pod,orders_vip, TCP, 80).
hacl(payment_vip,orders_pod, TCP, 80).
hacl(orders_pod,payment_vip, TCP, 80).
hacl(orders_pod,payment_pod, TCP, 80).
hacl(queue_master_pod,orders_vip, TCP, 80).
hacl(queue_master_vip,orders_pod, TCP, 80).
hacl(orders_pod,queue_master_vip, TCP, 80).
hacl(orders_pod,queue_master_pod, TCP, 80).
hacl(rabbitmq_pod,orders_vip, TCP, 80).
hacl(rabbitmq_vip,orders_pod, TCP, 80).
hacl(orders_pod,rabbitmq_vip, TCP, 80).
hacl(orders_pod,rabbitmq_pod, TCP, 80).
hacl(shipping_pod,orders_vip, TCP, 80).
hacl(shipping_vip,orders_pod, TCP, 80).
hacl(orders_pod,shipping_vip, TCP, 80).
hacl(orders_pod,shipping_pod, TCP, 80).
hacl(user_db_pod,orders_vip, TCP, 80).
hacl(user_db_vip,orders_pod, TCP, 80).
hacl(orders_pod,user_db_vip, TCP, 80).
hacl(orders_pod,user_db_pod, TCP, 80).
hacl(user_pod,orders_vip, TCP, 80).
hacl(user_vip,orders_pod, TCP, 80).
hacl(orders_pod,user_vip, TCP, 80).
hacl(orders_pod,user_pod, TCP, 80).
hacl(orders_pod,kube_dns_vip, TCP, 80).
hacl(internet,orders_vip, TCP, 80).
hacl(internet,orders_pod, TCP, 80).
hacl(orders_pod,internet, TCP, 80).
hacl(orders_pod,internet, TCP, 80).
hacl(orders_pod,orders_vip, TCP, 80).
hacl(orders_vip,orders_pod, _, _).
vulExists(orders_pod, 'attacker_can_access_anything', is_computer).
networkServiceInfo(orders_pod, is_computer,  _, _ , _).
vulExists(orders_vip, 'is_a_vip', is_vip).
networkServiceInfo(orders_vip, is_vip, TCP, 80, _).

hacl(carts_db_pod,payment_vip, TCP, 80).
hacl(carts_db_vip,payment_pod, TCP, 80).
hacl(payment_pod,carts_db_vip, TCP, 80).
hacl(payment_pod,carts_db_pod, TCP, 80).
hacl(cart_pod,payment_vip, TCP, 80).
hacl(cart_vip,payment_pod, TCP, 80).
hacl(payment_pod,cart_vip, TCP, 80).
hacl(payment_pod,cart_pod, TCP, 80).
hacl(catalogue_db_pod,payment_vip, TCP, 80).
hacl(catalogue_db_vip,payment_pod, TCP, 80).
hacl(payment_pod,catalogue_db_vip, TCP, 80).
hacl(payment_pod,catalogue_db_pod, TCP, 80).
hacl(catalogue_pod,payment_vip, TCP, 80).
hacl(catalogue_vip,payment_pod, TCP, 80).
hacl(payment_pod,catalogue_vip, TCP, 80).
hacl(payment_pod,catalogue_pod, TCP, 80).
hacl(front_end_pod,payment_vip, TCP, 80).
hacl(front_end_vip,payment_pod, TCP, 80).
hacl(payment_pod,front_end_vip, TCP, 80).
hacl(payment_pod,front_end_pod, TCP, 80).
hacl(orders_db_pod,payment_vip, TCP, 80).
hacl(orders_db_vip,payment_pod, TCP, 80).
hacl(payment_pod,orders_db_vip, TCP, 80).
hacl(payment_pod,orders_db_pod, TCP, 80).
hacl(orders_pod,payment_vip, TCP, 80).
hacl(orders_vip,payment_pod, TCP, 80).
hacl(payment_pod,orders_vip, TCP, 80).
hacl(payment_pod,orders_pod, TCP, 80).
hacl(queue_master_pod,payment_vip, TCP, 80).
hacl(queue_master_vip,payment_pod, TCP, 80).
hacl(payment_pod,queue_master_vip, TCP, 80).
hacl(payment_pod,queue_master_pod, TCP, 80).
hacl(rabbitmq_pod,payment_vip, TCP, 80).
hacl(rabbitmq_vip,payment_pod, TCP, 80).
hacl(payment_pod,rabbitmq_vip, TCP, 80).
hacl(payment_pod,rabbitmq_pod, TCP, 80).
hacl(shipping_pod,payment_vip, TCP, 80).
hacl(shipping_vip,payment_pod, TCP, 80).
hacl(payment_pod,shipping_vip, TCP, 80).
hacl(payment_pod,shipping_pod, TCP, 80).
hacl(user_db_pod,payment_vip, TCP, 80).
hacl(user_db_vip,payment_pod, TCP, 80).
hacl(payment_pod,user_db_vip, TCP, 80).
hacl(payment_pod,user_db_pod, TCP, 80).
hacl(user_pod,payment_vip, TCP, 80).
hacl(user_vip,payment_pod, TCP, 80).
hacl(payment_pod,user_vip, TCP, 80).
hacl(payment_pod,user_pod, TCP, 80).
hacl(payment_pod,kube_dns_vip, TCP, 80).
hacl(internet,payment_vip, TCP, 80).
hacl(internet,payment_pod, TCP, 80).
hacl(payment_pod,internet, TCP, 80).
hacl(payment_pod,internet, TCP, 80).
hacl(payment_pod,payment_vip, TCP, 80).
hacl(payment_vip,payment_pod, _, _).
vulExists(payment_pod, 'attacker_can_access_anything', is_computer).
networkServiceInfo(payment_pod, is_computer,  _, _ , _).
vulExists(payment_vip, 'is_a_vip', is_vip).
networkServiceInfo(payment_vip, is_vip, TCP, 80, _).

hacl(carts_db_pod,queue_master_vip, TCP, 80).
hacl(carts_db_vip,queue_master_pod, TCP, 80).
hacl(queue_master_pod,carts_db_vip, TCP, 80).
hacl(queue_master_pod,carts_db_pod, TCP, 80).
hacl(cart_pod,queue_master_vip, TCP, 80).
hacl(cart_vip,queue_master_pod, TCP, 80).
hacl(queue_master_pod,cart_vip, TCP, 80).
hacl(queue_master_pod,cart_pod, TCP, 80).
hacl(catalogue_db_pod,queue_master_vip, TCP, 80).
hacl(catalogue_db_vip,queue_master_pod, TCP, 80).
hacl(queue_master_pod,catalogue_db_vip, TCP, 80).
hacl(queue_master_pod,catalogue_db_pod, TCP, 80).
hacl(catalogue_pod,queue_master_vip, TCP, 80).
hacl(catalogue_vip,queue_master_pod, TCP, 80).
hacl(queue_master_pod,catalogue_vip, TCP, 80).
hacl(queue_master_pod,catalogue_pod, TCP, 80).
hacl(front_end_pod,queue_master_vip, TCP, 80).
hacl(front_end_vip,queue_master_pod, TCP, 80).
hacl(queue_master_pod,front_end_vip, TCP, 80).
hacl(queue_master_pod,front_end_pod, TCP, 80).
hacl(orders_db_pod,queue_master_vip, TCP, 80).
hacl(orders_db_vip,queue_master_pod, TCP, 80).
hacl(queue_master_pod,orders_db_vip, TCP, 80).
hacl(queue_master_pod,orders_db_pod, TCP, 80).
hacl(orders_pod,queue_master_vip, TCP, 80).
hacl(orders_vip,queue_master_pod, TCP, 80).
hacl(queue_master_pod,orders_vip, TCP, 80).
hacl(queue_master_pod,orders_pod, TCP, 80).
hacl(payment_pod,queue_master_vip, TCP, 80).
hacl(payment_vip,queue_master_pod, TCP, 80).
hacl(queue_master_pod,payment_vip, TCP, 80).
hacl(queue_master_pod,payment_pod, TCP, 80).
hacl(rabbitmq_pod,queue_master_vip, TCP, 80).
hacl(rabbitmq_vip,queue_master_pod, TCP, 80).
hacl(queue_master_pod,rabbitmq_vip, TCP, 80).
hacl(queue_master_pod,rabbitmq_pod, TCP, 80).
hacl(shipping_pod,queue_master_vip, TCP, 80).
hacl(shipping_vip,queue_master_pod, TCP, 80).
hacl(queue_master_pod,shipping_vip, TCP, 80).
hacl(queue_master_pod,shipping_pod, TCP, 80).
hacl(user_db_pod,queue_master_vip, TCP, 80).
hacl(user_db_vip,queue_master_pod, TCP, 80).
hacl(queue_master_pod,user_db_vip, TCP, 80).
hacl(queue_master_pod,user_db_pod, TCP, 80).
hacl(user_pod,queue_master_vip, TCP, 80).
hacl(user_vip,queue_master_pod, TCP, 80).
hacl(queue_master_pod,user_vip, TCP, 80).
hacl(queue_master_pod,user_pod, TCP, 80).
hacl(queue_master_pod,kube_dns_vip, TCP, 80).
hacl(internet,queue_master_vip, TCP, 80).
hacl(internet,queue_master_pod, TCP, 80).
hacl(queue_master_pod,internet, TCP, 80).
hacl(queue_master_pod,internet, TCP, 80).
hacl(queue_master_pod,queue_master_vip, TCP, 80).
hacl(queue_master_vip,queue_master_pod, _, _).
vulExists(queue_master_pod, 'attacker_can_access_anything', is_computer).
networkServiceInfo(queue_master_pod, is_computer,  _, _ , _).
vulExists(queue_master_vip, 'is_a_vip', is_vip).
networkServiceInfo(queue_master_vip, is_vip, TCP, 80, _).

hacl(carts_db_pod,rabbitmq_vip, TCP, 5672).
hacl(carts_db_vip,rabbitmq_pod, TCP, 5672).
hacl(rabbitmq_pod,carts_db_vip, TCP, 5672).
hacl(rabbitmq_pod,carts_db_pod, TCP, 5672).
hacl(cart_pod,rabbitmq_vip, TCP, 5672).
hacl(cart_vip,rabbitmq_pod, TCP, 5672).
hacl(rabbitmq_pod,cart_vip, TCP, 5672).
hacl(rabbitmq_pod,cart_pod, TCP, 5672).
hacl(catalogue_db_pod,rabbitmq_vip, TCP, 5672).
hacl(catalogue_db_vip,rabbitmq_pod, TCP, 5672).
hacl(rabbitmq_pod,catalogue_db_vip, TCP, 5672).
hacl(rabbitmq_pod,catalogue_db_pod, TCP, 5672).
hacl(catalogue_pod,rabbitmq_vip, TCP, 5672).
hacl(catalogue_vip,rabbitmq_pod, TCP, 5672).
hacl(rabbitmq_pod,catalogue_vip, TCP, 5672).
hacl(rabbitmq_pod,catalogue_pod, TCP, 5672).
hacl(front_end_pod,rabbitmq_vip, TCP, 5672).
hacl(front_end_vip,rabbitmq_pod, TCP, 5672).
hacl(rabbitmq_pod,front_end_vip, TCP, 5672).
hacl(rabbitmq_pod,front_end_pod, TCP, 5672).
hacl(orders_db_pod,rabbitmq_vip, TCP, 5672).
hacl(orders_db_vip,rabbitmq_pod, TCP, 5672).
hacl(rabbitmq_pod,orders_db_vip, TCP, 5672).
hacl(rabbitmq_pod,orders_db_pod, TCP, 5672).
hacl(orders_pod,rabbitmq_vip, TCP, 5672).
hacl(orders_vip,rabbitmq_pod, TCP, 5672).
hacl(rabbitmq_pod,orders_vip, TCP, 5672).
hacl(rabbitmq_pod,orders_pod, TCP, 5672).
hacl(payment_pod,rabbitmq_vip, TCP, 5672).
hacl(payment_vip,rabbitmq_pod, TCP, 5672).
hacl(rabbitmq_pod,payment_vip, TCP, 5672).
hacl(rabbitmq_pod,payment_pod, TCP, 5672).
hacl(queue_master_pod,rabbitmq_vip, TCP, 5672).
hacl(queue_master_vip,rabbitmq_pod, TCP, 5672).
hacl(rabbitmq_pod,queue_master_vip, TCP, 5672).
hacl(rabbitmq_pod,queue_master_pod, TCP, 5672).
hacl(shipping_pod,rabbitmq_vip, TCP, 5672).
hacl(shipping_vip,rabbitmq_pod, TCP, 5672).
hacl(rabbitmq_pod,shipping_vip, TCP, 5672).
hacl(rabbitmq_pod,shipping_pod, TCP, 5672).
hacl(user_db_pod,rabbitmq_vip, TCP, 5672).
hacl(user_db_vip,rabbitmq_pod, TCP, 5672).
hacl(rabbitmq_pod,user_db_vip, TCP, 5672).
hacl(rabbitmq_pod,user_db_pod, TCP, 5672).
hacl(user_pod,rabbitmq_vip, TCP, 5672).
hacl(user_vip,rabbitmq_pod, TCP, 5672).
hacl(rabbitmq_pod,user_vip, TCP, 5672).
hacl(rabbitmq_pod,user_pod, TCP, 5672).
hacl(rabbitmq_pod,kube_dns_vip, TCP, 5672).
hacl(internet,rabbitmq_vip, TCP, 5672).
hacl(internet,rabbitmq_pod, TCP, 5672).
hacl(rabbitmq_pod,internet, TCP, 5672).
hacl(rabbitmq_pod,internet, TCP, 5672).
hacl(rabbitmq_pod,rabbitmq_vip, TCP, 5672).
hacl(rabbitmq_vip,rabbitmq_pod, _, _).
vulExists(rabbitmq_pod, 'attacker_can_access_anything', is_computer).
networkServiceInfo(rabbitmq_pod, is_computer,  _, _ , _).
vulExists(rabbitmq_vip, 'is_a_vip', is_vip).
networkServiceInfo(rabbitmq_vip, is_vip, TCP, 5672, _).

hacl(carts_db_pod,shipping_vip, TCP, 80).
hacl(carts_db_vip,shipping_pod, TCP, 80).
hacl(shipping_pod,carts_db_vip, TCP, 80).
hacl(shipping_pod,carts_db_pod, TCP, 80).
hacl(cart_pod,shipping_vip, TCP, 80).
hacl(cart_vip,shipping_pod, TCP, 80).
hacl(shipping_pod,cart_vip, TCP, 80).
hacl(shipping_pod,cart_pod, TCP, 80).
hacl(catalogue_db_pod,shipping_vip, TCP, 80).
hacl(catalogue_db_vip,shipping_pod, TCP, 80).
hacl(shipping_pod,catalogue_db_vip, TCP, 80).
hacl(shipping_pod,catalogue_db_pod, TCP, 80).
hacl(catalogue_pod,shipping_vip, TCP, 80).
hacl(catalogue_vip,shipping_pod, TCP, 80).
hacl(shipping_pod,catalogue_vip, TCP, 80).
hacl(shipping_pod,catalogue_pod, TCP, 80).
hacl(front_end_pod,shipping_vip, TCP, 80).
hacl(front_end_vip,shipping_pod, TCP, 80).
hacl(shipping_pod,front_end_vip, TCP, 80).
hacl(shipping_pod,front_end_pod, TCP, 80).
hacl(orders_db_pod,shipping_vip, TCP, 80).
hacl(orders_db_vip,shipping_pod, TCP, 80).
hacl(shipping_pod,orders_db_vip, TCP, 80).
hacl(shipping_pod,orders_db_pod, TCP, 80).
hacl(orders_pod,shipping_vip, TCP, 80).
hacl(orders_vip,shipping_pod, TCP, 80).
hacl(shipping_pod,orders_vip, TCP, 80).
hacl(shipping_pod,orders_pod, TCP, 80).
hacl(payment_pod,shipping_vip, TCP, 80).
hacl(payment_vip,shipping_pod, TCP, 80).
hacl(shipping_pod,payment_vip, TCP, 80).
hacl(shipping_pod,payment_pod, TCP, 80).
hacl(queue_master_pod,shipping_vip, TCP, 80).
hacl(queue_master_vip,shipping_pod, TCP, 80).
hacl(shipping_pod,queue_master_vip, TCP, 80).
hacl(shipping_pod,queue_master_pod, TCP, 80).
hacl(rabbitmq_pod,shipping_vip, TCP, 80).
hacl(rabbitmq_vip,shipping_pod, TCP, 80).
hacl(shipping_pod,rabbitmq_vip, TCP, 80).
hacl(shipping_pod,rabbitmq_pod, TCP, 80).
hacl(user_db_pod,shipping_vip, TCP, 80).
hacl(user_db_vip,shipping_pod, TCP, 80).
hacl(shipping_pod,user_db_vip, TCP, 80).
hacl(shipping_pod,user_db_pod, TCP, 80).
hacl(user_pod,shipping_vip, TCP, 80).
hacl(user_vip,shipping_pod, TCP, 80).
hacl(shipping_pod,user_vip, TCP, 80).
hacl(shipping_pod,user_pod, TCP, 80).
hacl(shipping_pod,kube_dns_vip, TCP, 80).
hacl(internet,shipping_vip, TCP, 80).
hacl(internet,shipping_pod, TCP, 80).
hacl(shipping_pod,internet, TCP, 80).
hacl(shipping_pod,internet, TCP, 80).
hacl(shipping_pod,shipping_vip, TCP, 80).
hacl(shipping_vip,shipping_pod, _, _).
vulExists(shipping_pod, 'attacker_can_access_anything', is_computer).
networkServiceInfo(shipping_pod, is_computer,  _, _ , _).
vulExists(shipping_vip, 'is_a_vip', is_vip).
networkServiceInfo(shipping_vip, is_vip, TCP, 80, _).
attackerLocated(user_db_pod).

hacl(carts_db_pod,user_db_vip, TCP, 27017).
hacl(carts_db_vip,user_db_pod, TCP, 27017).
hacl(user_db_pod,carts_db_vip, TCP, 27017).
hacl(user_db_pod,carts_db_pod, TCP, 27017).
hacl(cart_pod,user_db_vip, TCP, 27017).
hacl(cart_vip,user_db_pod, TCP, 27017).
hacl(user_db_pod,cart_vip, TCP, 27017).
hacl(user_db_pod,cart_pod, TCP, 27017).
hacl(catalogue_db_pod,user_db_vip, TCP, 27017).
hacl(catalogue_db_vip,user_db_pod, TCP, 27017).
hacl(user_db_pod,catalogue_db_vip, TCP, 27017).
hacl(user_db_pod,catalogue_db_pod, TCP, 27017).
hacl(catalogue_pod,user_db_vip, TCP, 27017).
hacl(catalogue_vip,user_db_pod, TCP, 27017).
hacl(user_db_pod,catalogue_vip, TCP, 27017).
hacl(user_db_pod,catalogue_pod, TCP, 27017).
hacl(front_end_pod,user_db_vip, TCP, 27017).
hacl(front_end_vip,user_db_pod, TCP, 27017).
hacl(user_db_pod,front_end_vip, TCP, 27017).
hacl(user_db_pod,front_end_pod, TCP, 27017).
hacl(orders_db_pod,user_db_vip, TCP, 27017).
hacl(orders_db_vip,user_db_pod, TCP, 27017).
hacl(user_db_pod,orders_db_vip, TCP, 27017).
hacl(user_db_pod,orders_db_pod, TCP, 27017).
hacl(orders_pod,user_db_vip, TCP, 27017).
hacl(orders_vip,user_db_pod, TCP, 27017).
hacl(user_db_pod,orders_vip, TCP, 27017).
hacl(user_db_pod,orders_pod, TCP, 27017).
hacl(payment_pod,user_db_vip, TCP, 27017).
hacl(payment_vip,user_db_pod, TCP, 27017).
hacl(user_db_pod,payment_vip, TCP, 27017).
hacl(user_db_pod,payment_pod, TCP, 27017).
hacl(queue_master_pod,user_db_vip, TCP, 27017).
hacl(queue_master_vip,user_db_pod, TCP, 27017).
hacl(user_db_pod,queue_master_vip, TCP, 27017).
hacl(user_db_pod,queue_master_pod, TCP, 27017).
hacl(rabbitmq_pod,user_db_vip, TCP, 27017).
hacl(rabbitmq_vip,user_db_pod, TCP, 27017).
hacl(user_db_pod,rabbitmq_vip, TCP, 27017).
hacl(user_db_pod,rabbitmq_pod, TCP, 27017).
hacl(shipping_pod,user_db_vip, TCP, 27017).
hacl(shipping_vip,user_db_pod, TCP, 27017).
hacl(user_db_pod,shipping_vip, TCP, 27017).
hacl(user_db_pod,shipping_pod, TCP, 27017).
hacl(user_pod,user_db_vip, TCP, 27017).
hacl(user_vip,user_db_pod, TCP, 27017).
hacl(user_db_pod,user_vip, TCP, 27017).
hacl(user_db_pod,user_pod, TCP, 27017).
hacl(user_db_pod,kube_dns_vip, TCP, 27017).
hacl(internet,user_db_vip, TCP, 27017).
hacl(internet,user_db_pod, TCP, 27017).
hacl(user_db_pod,internet, TCP, 27017).
hacl(user_db_pod,internet, TCP, 27017).
hacl(user_db_pod,user_db_vip, TCP, 27017).
hacl(user_db_vip,user_db_pod, _, _).
vulExists(user_db_pod, 'attacker_can_access_anything', is_computer).
networkServiceInfo(user_db_pod, is_computer,  _, _ , _).
vulExists(user_db_vip, 'is_a_vip', is_vip).
networkServiceInfo(user_db_vip, is_vip, TCP, 27017, _).

hacl(carts_db_pod,user_vip, TCP, 27017).
hacl(carts_db_vip,user_pod, TCP, 27017).
hacl(user_pod,carts_db_vip, TCP, 27017).
hacl(user_pod,carts_db_pod, TCP, 27017).
hacl(cart_pod,user_vip, TCP, 27017).
hacl(cart_vip,user_pod, TCP, 27017).
hacl(user_pod,cart_vip, TCP, 27017).
hacl(user_pod,cart_pod, TCP, 27017).
hacl(catalogue_db_pod,user_vip, TCP, 27017).
hacl(catalogue_db_vip,user_pod, TCP, 27017).
hacl(user_pod,catalogue_db_vip, TCP, 27017).
hacl(user_pod,catalogue_db_pod, TCP, 27017).
hacl(catalogue_pod,user_vip, TCP, 27017).
hacl(catalogue_vip,user_pod, TCP, 27017).
hacl(user_pod,catalogue_vip, TCP, 27017).
hacl(user_pod,catalogue_pod, TCP, 27017).
hacl(front_end_pod,user_vip, TCP, 27017).
hacl(front_end_vip,user_pod, TCP, 27017).
hacl(user_pod,front_end_vip, TCP, 27017).
hacl(user_pod,front_end_pod, TCP, 27017).
hacl(orders_db_pod,user_vip, TCP, 27017).
hacl(orders_db_vip,user_pod, TCP, 27017).
hacl(user_pod,orders_db_vip, TCP, 27017).
hacl(user_pod,orders_db_pod, TCP, 27017).
hacl(orders_pod,user_vip, TCP, 27017).
hacl(orders_vip,user_pod, TCP, 27017).
hacl(user_pod,orders_vip, TCP, 27017).
hacl(user_pod,orders_pod, TCP, 27017).
hacl(payment_pod,user_vip, TCP, 27017).
hacl(payment_vip,user_pod, TCP, 27017).
hacl(user_pod,payment_vip, TCP, 27017).
hacl(user_pod,payment_pod, TCP, 27017).
hacl(queue_master_pod,user_vip, TCP, 27017).
hacl(queue_master_vip,user_pod, TCP, 27017).
hacl(user_pod,queue_master_vip, TCP, 27017).
hacl(user_pod,queue_master_pod, TCP, 27017).
hacl(rabbitmq_pod,user_vip, TCP, 27017).
hacl(rabbitmq_vip,user_pod, TCP, 27017).
hacl(user_pod,rabbitmq_vip, TCP, 27017).
hacl(user_pod,rabbitmq_pod, TCP, 27017).
hacl(shipping_pod,user_vip, TCP, 27017).
hacl(shipping_vip,user_pod, TCP, 27017).
hacl(user_pod,shipping_vip, TCP, 27017).
hacl(user_pod,shipping_pod, TCP, 27017).
hacl(user_db_pod,user_vip, TCP, 27017).
hacl(user_db_vip,user_pod, TCP, 27017).
hacl(user_pod,user_db_vip, TCP, 27017).
hacl(user_pod,user_db_pod, TCP, 27017).
hacl(user_pod,kube_dns_vip, TCP, 27017).
hacl(internet,user_vip, TCP, 27017).
hacl(internet,user_pod, TCP, 27017).
hacl(user_pod,internet, TCP, 27017).
hacl(user_pod,internet, TCP, 27017).
hacl(user_pod,user_vip, TCP, 27017).
hacl(user_vip,user_pod, _, _).
vulExists(user_pod, 'attacker_can_access_anything', is_computer).
networkServiceInfo(user_pod, is_computer,  _, _ , _).
vulExists(user_vip, 'is_a_vip', is_vip).
networkServiceInfo(user_vip, is_vip, TCP, 27017, _).

hacl(carts_db_pod,kube_dns_vip, UDP, 53).
hacl(cart_pod,kube_dns_vip, UDP, 53).
hacl(catalogue_db_pod,kube_dns_vip, UDP, 53).
hacl(catalogue_pod,kube_dns_vip, UDP, 53).
hacl(front_end_pod,kube_dns_vip, UDP, 53).
hacl(orders_db_pod,kube_dns_vip, UDP, 53).
hacl(orders_pod,kube_dns_vip, UDP, 53).
hacl(payment_pod,kube_dns_vip, UDP, 53).
hacl(queue_master_pod,kube_dns_vip, UDP, 53).
hacl(rabbitmq_pod,kube_dns_vip, UDP, 53).
hacl(shipping_pod,kube_dns_vip, UDP, 53).
hacl(user_db_pod,kube_dns_vip, UDP, 53).
hacl(user_pod,kube_dns_vip, UDP, 53).
hacl(internet,kube_dns_vip, UDP, 53).
hacl(kube_dns_pod,kube_dns_vip, UDP, 53).
hacl(kube_dns_vip,kube_dns_pod, _, _).
vulExists(kube_dns_pod, 'attacker_can_access_anything', is_computer).
networkServiceInfo(kube_dns_pod, is_computer,  _, _ , _).
vulExists(kube_dns_vip, 'is_a_vip', is_vip).
networkServiceInfo(kube_dns_vip, is_vip, UDP, 53, _).
hacl(kube_dns_pod,internet, UDP, 53).
