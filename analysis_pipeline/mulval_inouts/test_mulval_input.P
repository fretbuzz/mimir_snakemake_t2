%autogenerated
attackGoal(execCode(internet, _)).
vulExists(internet, 'attacker_can_access_anything', is_computer).
networkServiceInfo(internet, is_computer, _, _, _).
vulProperty('attacker_can_access_anything', remoteExploit, privEscalation).

hacl(front_end_pod,rabbitmq_pod, _, _).
hacl(front_end_pod,carts_db_pod, _, _).
hacl(front_end_pod,kubernetes_pod, _, _).
hacl(front_end_pod,heapster_pod, _, _).
hacl(front_end_pod,queue_master_pod, _, _).
hacl(front_end_pod,monitoring_grafana_pod, _, _).
hacl(front_end_pod,kube_dns_pod, _, _).
hacl(front_end_pod,shipping_pod, _, _).
hacl(front_end_pod,payment_pod, _, _).
hacl(front_end_pod,carts_pod, _, _).
hacl(front_end_pod,catalogue_pod, _, _).
hacl(front_end_pod,kubernetes_dashboard_pod, _, _).
hacl(front_end_pod,user_pod, _, _).
hacl(front_end_pod,monitoring_influxdb_pod, _, _).
hacl(front_end_pod,orders_db_pod, _, _).
hacl(front_end_pod,metrics_server_pod, _, _).
hacl(front_end_pod,orders_pod, _, _).
hacl(front_end_pod,catalogue_db_pod, _, _).
hacl(front_end_pod,user_db_pod, _, _).
hacl(front_end_pod,internet, _, _).
vulExists(front_end_pod, 'attacker_can_access_anything', is_computer).
networkServiceInfo(front_end_pod, is_computer,  _, _ , _).

hacl(rabbitmq_pod,front_end_pod, _, _).
hacl(rabbitmq_pod,carts_db_pod, _, _).
hacl(rabbitmq_pod,kubernetes_pod, _, _).
hacl(rabbitmq_pod,heapster_pod, _, _).
hacl(rabbitmq_pod,queue_master_pod, _, _).
hacl(rabbitmq_pod,monitoring_grafana_pod, _, _).
hacl(rabbitmq_pod,kube_dns_pod, _, _).
hacl(rabbitmq_pod,shipping_pod, _, _).
hacl(rabbitmq_pod,payment_pod, _, _).
hacl(rabbitmq_pod,carts_pod, _, _).
hacl(rabbitmq_pod,catalogue_pod, _, _).
hacl(rabbitmq_pod,kubernetes_dashboard_pod, _, _).
hacl(rabbitmq_pod,user_pod, _, _).
hacl(rabbitmq_pod,monitoring_influxdb_pod, _, _).
hacl(rabbitmq_pod,orders_db_pod, _, _).
hacl(rabbitmq_pod,metrics_server_pod, _, _).
hacl(rabbitmq_pod,orders_pod, _, _).
hacl(rabbitmq_pod,catalogue_db_pod, _, _).
hacl(rabbitmq_pod,user_db_pod, _, _).
hacl(rabbitmq_pod,internet, _, _).
vulExists(rabbitmq_pod, 'attacker_can_access_anything', is_computer).
networkServiceInfo(rabbitmq_pod, is_computer,  _, _ , _).

hacl(carts_db_pod,front_end_pod, _, _).
hacl(carts_db_pod,rabbitmq_pod, _, _).
hacl(carts_db_pod,kubernetes_pod, _, _).
hacl(carts_db_pod,heapster_pod, _, _).
hacl(carts_db_pod,queue_master_pod, _, _).
hacl(carts_db_pod,monitoring_grafana_pod, _, _).
hacl(carts_db_pod,kube_dns_pod, _, _).
hacl(carts_db_pod,shipping_pod, _, _).
hacl(carts_db_pod,payment_pod, _, _).
hacl(carts_db_pod,carts_pod, _, _).
hacl(carts_db_pod,catalogue_pod, _, _).
hacl(carts_db_pod,kubernetes_dashboard_pod, _, _).
hacl(carts_db_pod,user_pod, _, _).
hacl(carts_db_pod,monitoring_influxdb_pod, _, _).
hacl(carts_db_pod,orders_db_pod, _, _).
hacl(carts_db_pod,metrics_server_pod, _, _).
hacl(carts_db_pod,orders_pod, _, _).
hacl(carts_db_pod,catalogue_db_pod, _, _).
hacl(carts_db_pod,user_db_pod, _, _).
hacl(carts_db_pod,internet, _, _).
vulExists(carts_db_pod, 'attacker_can_access_anything', is_computer).
networkServiceInfo(carts_db_pod, is_computer,  _, _ , _).

hacl(kubernetes_pod,front_end_pod, _, _).
hacl(kubernetes_pod,rabbitmq_pod, _, _).
hacl(kubernetes_pod,carts_db_pod, _, _).
hacl(kubernetes_pod,heapster_pod, _, _).
hacl(kubernetes_pod,queue_master_pod, _, _).
hacl(kubernetes_pod,monitoring_grafana_pod, _, _).
hacl(kubernetes_pod,kube_dns_pod, _, _).
hacl(kubernetes_pod,shipping_pod, _, _).
hacl(kubernetes_pod,payment_pod, _, _).
hacl(kubernetes_pod,carts_pod, _, _).
hacl(kubernetes_pod,catalogue_pod, _, _).
hacl(kubernetes_pod,kubernetes_dashboard_pod, _, _).
hacl(kubernetes_pod,user_pod, _, _).
hacl(kubernetes_pod,monitoring_influxdb_pod, _, _).
hacl(kubernetes_pod,orders_db_pod, _, _).
hacl(kubernetes_pod,metrics_server_pod, _, _).
hacl(kubernetes_pod,orders_pod, _, _).
hacl(kubernetes_pod,catalogue_db_pod, _, _).
hacl(kubernetes_pod,user_db_pod, _, _).
hacl(kubernetes_pod,internet, _, _).
vulExists(kubernetes_pod, 'attacker_can_access_anything', is_computer).
networkServiceInfo(kubernetes_pod, is_computer,  _, _ , _).

hacl(heapster_pod,front_end_pod, _, _).
hacl(heapster_pod,rabbitmq_pod, _, _).
hacl(heapster_pod,carts_db_pod, _, _).
hacl(heapster_pod,kubernetes_pod, _, _).
hacl(heapster_pod,queue_master_pod, _, _).
hacl(heapster_pod,monitoring_grafana_pod, _, _).
hacl(heapster_pod,kube_dns_pod, _, _).
hacl(heapster_pod,shipping_pod, _, _).
hacl(heapster_pod,payment_pod, _, _).
hacl(heapster_pod,carts_pod, _, _).
hacl(heapster_pod,catalogue_pod, _, _).
hacl(heapster_pod,kubernetes_dashboard_pod, _, _).
hacl(heapster_pod,user_pod, _, _).
hacl(heapster_pod,monitoring_influxdb_pod, _, _).
hacl(heapster_pod,orders_db_pod, _, _).
hacl(heapster_pod,metrics_server_pod, _, _).
hacl(heapster_pod,orders_pod, _, _).
hacl(heapster_pod,catalogue_db_pod, _, _).
hacl(heapster_pod,user_db_pod, _, _).
hacl(heapster_pod,internet, _, _).
vulExists(heapster_pod, 'attacker_can_access_anything', is_computer).
networkServiceInfo(heapster_pod, is_computer,  _, _ , _).

hacl(queue_master_pod,front_end_pod, _, _).
hacl(queue_master_pod,rabbitmq_pod, _, _).
hacl(queue_master_pod,carts_db_pod, _, _).
hacl(queue_master_pod,kubernetes_pod, _, _).
hacl(queue_master_pod,heapster_pod, _, _).
hacl(queue_master_pod,monitoring_grafana_pod, _, _).
hacl(queue_master_pod,kube_dns_pod, _, _).
hacl(queue_master_pod,shipping_pod, _, _).
hacl(queue_master_pod,payment_pod, _, _).
hacl(queue_master_pod,carts_pod, _, _).
hacl(queue_master_pod,catalogue_pod, _, _).
hacl(queue_master_pod,kubernetes_dashboard_pod, _, _).
hacl(queue_master_pod,user_pod, _, _).
hacl(queue_master_pod,monitoring_influxdb_pod, _, _).
hacl(queue_master_pod,orders_db_pod, _, _).
hacl(queue_master_pod,metrics_server_pod, _, _).
hacl(queue_master_pod,orders_pod, _, _).
hacl(queue_master_pod,catalogue_db_pod, _, _).
hacl(queue_master_pod,user_db_pod, _, _).
hacl(queue_master_pod,internet, _, _).
vulExists(queue_master_pod, 'attacker_can_access_anything', is_computer).
networkServiceInfo(queue_master_pod, is_computer,  _, _ , _).

hacl(monitoring_grafana_pod,front_end_pod, _, _).
hacl(monitoring_grafana_pod,rabbitmq_pod, _, _).
hacl(monitoring_grafana_pod,carts_db_pod, _, _).
hacl(monitoring_grafana_pod,kubernetes_pod, _, _).
hacl(monitoring_grafana_pod,heapster_pod, _, _).
hacl(monitoring_grafana_pod,queue_master_pod, _, _).
hacl(monitoring_grafana_pod,kube_dns_pod, _, _).
hacl(monitoring_grafana_pod,shipping_pod, _, _).
hacl(monitoring_grafana_pod,payment_pod, _, _).
hacl(monitoring_grafana_pod,carts_pod, _, _).
hacl(monitoring_grafana_pod,catalogue_pod, _, _).
hacl(monitoring_grafana_pod,kubernetes_dashboard_pod, _, _).
hacl(monitoring_grafana_pod,user_pod, _, _).
hacl(monitoring_grafana_pod,monitoring_influxdb_pod, _, _).
hacl(monitoring_grafana_pod,orders_db_pod, _, _).
hacl(monitoring_grafana_pod,metrics_server_pod, _, _).
hacl(monitoring_grafana_pod,orders_pod, _, _).
hacl(monitoring_grafana_pod,catalogue_db_pod, _, _).
hacl(monitoring_grafana_pod,user_db_pod, _, _).
hacl(monitoring_grafana_pod,internet, _, _).
vulExists(monitoring_grafana_pod, 'attacker_can_access_anything', is_computer).
networkServiceInfo(monitoring_grafana_pod, is_computer,  _, _ , _).

vulExists(kube_dns_pod, 'attacker_can_access_anything', is_computer).
networkServiceInfo(kube_dns_pod, is_computer,  _, _ , _).

hacl(shipping_pod,front_end_pod, _, _).
hacl(shipping_pod,rabbitmq_pod, _, _).
hacl(shipping_pod,carts_db_pod, _, _).
hacl(shipping_pod,kubernetes_pod, _, _).
hacl(shipping_pod,heapster_pod, _, _).
hacl(shipping_pod,queue_master_pod, _, _).
hacl(shipping_pod,monitoring_grafana_pod, _, _).
hacl(shipping_pod,kube_dns_pod, _, _).
hacl(shipping_pod,payment_pod, _, _).
hacl(shipping_pod,carts_pod, _, _).
hacl(shipping_pod,catalogue_pod, _, _).
hacl(shipping_pod,kubernetes_dashboard_pod, _, _).
hacl(shipping_pod,user_pod, _, _).
hacl(shipping_pod,monitoring_influxdb_pod, _, _).
hacl(shipping_pod,orders_db_pod, _, _).
hacl(shipping_pod,metrics_server_pod, _, _).
hacl(shipping_pod,orders_pod, _, _).
hacl(shipping_pod,catalogue_db_pod, _, _).
hacl(shipping_pod,user_db_pod, _, _).
hacl(shipping_pod,internet, _, _).
vulExists(shipping_pod, 'attacker_can_access_anything', is_computer).
networkServiceInfo(shipping_pod, is_computer,  _, _ , _).

hacl(payment_pod,front_end_pod, _, _).
hacl(payment_pod,rabbitmq_pod, _, _).
hacl(payment_pod,carts_db_pod, _, _).
hacl(payment_pod,kubernetes_pod, _, _).
hacl(payment_pod,heapster_pod, _, _).
hacl(payment_pod,queue_master_pod, _, _).
hacl(payment_pod,monitoring_grafana_pod, _, _).
hacl(payment_pod,kube_dns_pod, _, _).
hacl(payment_pod,shipping_pod, _, _).
hacl(payment_pod,carts_pod, _, _).
hacl(payment_pod,catalogue_pod, _, _).
hacl(payment_pod,kubernetes_dashboard_pod, _, _).
hacl(payment_pod,user_pod, _, _).
hacl(payment_pod,monitoring_influxdb_pod, _, _).
hacl(payment_pod,orders_db_pod, _, _).
hacl(payment_pod,metrics_server_pod, _, _).
hacl(payment_pod,orders_pod, _, _).
hacl(payment_pod,catalogue_db_pod, _, _).
hacl(payment_pod,user_db_pod, _, _).
hacl(payment_pod,internet, _, _).
vulExists(payment_pod, 'attacker_can_access_anything', is_computer).
networkServiceInfo(payment_pod, is_computer,  _, _ , _).

hacl(carts_pod,front_end_pod, _, _).
hacl(carts_pod,rabbitmq_pod, _, _).
hacl(carts_pod,carts_db_pod, _, _).
hacl(carts_pod,kubernetes_pod, _, _).
hacl(carts_pod,heapster_pod, _, _).
hacl(carts_pod,queue_master_pod, _, _).
hacl(carts_pod,monitoring_grafana_pod, _, _).
hacl(carts_pod,kube_dns_pod, _, _).
hacl(carts_pod,shipping_pod, _, _).
hacl(carts_pod,payment_pod, _, _).
hacl(carts_pod,catalogue_pod, _, _).
hacl(carts_pod,kubernetes_dashboard_pod, _, _).
hacl(carts_pod,user_pod, _, _).
hacl(carts_pod,monitoring_influxdb_pod, _, _).
hacl(carts_pod,orders_db_pod, _, _).
hacl(carts_pod,metrics_server_pod, _, _).
hacl(carts_pod,orders_pod, _, _).
hacl(carts_pod,catalogue_db_pod, _, _).
hacl(carts_pod,user_db_pod, _, _).
hacl(carts_pod,internet, _, _).
vulExists(carts_pod, 'attacker_can_access_anything', is_computer).
networkServiceInfo(carts_pod, is_computer,  _, _ , _).

hacl(catalogue_pod,front_end_pod, _, _).
hacl(catalogue_pod,rabbitmq_pod, _, _).
hacl(catalogue_pod,carts_db_pod, _, _).
hacl(catalogue_pod,kubernetes_pod, _, _).
hacl(catalogue_pod,heapster_pod, _, _).
hacl(catalogue_pod,queue_master_pod, _, _).
hacl(catalogue_pod,monitoring_grafana_pod, _, _).
hacl(catalogue_pod,kube_dns_pod, _, _).
hacl(catalogue_pod,shipping_pod, _, _).
hacl(catalogue_pod,payment_pod, _, _).
hacl(catalogue_pod,carts_pod, _, _).
hacl(catalogue_pod,kubernetes_dashboard_pod, _, _).
hacl(catalogue_pod,user_pod, _, _).
hacl(catalogue_pod,monitoring_influxdb_pod, _, _).
hacl(catalogue_pod,orders_db_pod, _, _).
hacl(catalogue_pod,metrics_server_pod, _, _).
hacl(catalogue_pod,orders_pod, _, _).
hacl(catalogue_pod,catalogue_db_pod, _, _).
hacl(catalogue_pod,user_db_pod, _, _).
hacl(catalogue_pod,internet, _, _).
vulExists(catalogue_pod, 'attacker_can_access_anything', is_computer).
networkServiceInfo(catalogue_pod, is_computer,  _, _ , _).

hacl(kubernetes_dashboard_pod,front_end_pod, _, _).
hacl(kubernetes_dashboard_pod,rabbitmq_pod, _, _).
hacl(kubernetes_dashboard_pod,carts_db_pod, _, _).
hacl(kubernetes_dashboard_pod,kubernetes_pod, _, _).
hacl(kubernetes_dashboard_pod,heapster_pod, _, _).
hacl(kubernetes_dashboard_pod,queue_master_pod, _, _).
hacl(kubernetes_dashboard_pod,monitoring_grafana_pod, _, _).
hacl(kubernetes_dashboard_pod,kube_dns_pod, _, _).
hacl(kubernetes_dashboard_pod,shipping_pod, _, _).
hacl(kubernetes_dashboard_pod,payment_pod, _, _).
hacl(kubernetes_dashboard_pod,carts_pod, _, _).
hacl(kubernetes_dashboard_pod,catalogue_pod, _, _).
hacl(kubernetes_dashboard_pod,user_pod, _, _).
hacl(kubernetes_dashboard_pod,monitoring_influxdb_pod, _, _).
hacl(kubernetes_dashboard_pod,orders_db_pod, _, _).
hacl(kubernetes_dashboard_pod,metrics_server_pod, _, _).
hacl(kubernetes_dashboard_pod,orders_pod, _, _).
hacl(kubernetes_dashboard_pod,catalogue_db_pod, _, _).
hacl(kubernetes_dashboard_pod,user_db_pod, _, _).
hacl(kubernetes_dashboard_pod,internet, _, _).
vulExists(kubernetes_dashboard_pod, 'attacker_can_access_anything', is_computer).
networkServiceInfo(kubernetes_dashboard_pod, is_computer,  _, _ , _).

hacl(user_pod,front_end_pod, _, _).
hacl(user_pod,rabbitmq_pod, _, _).
hacl(user_pod,carts_db_pod, _, _).
hacl(user_pod,kubernetes_pod, _, _).
hacl(user_pod,heapster_pod, _, _).
hacl(user_pod,queue_master_pod, _, _).
hacl(user_pod,monitoring_grafana_pod, _, _).
hacl(user_pod,kube_dns_pod, _, _).
hacl(user_pod,shipping_pod, _, _).
hacl(user_pod,payment_pod, _, _).
hacl(user_pod,carts_pod, _, _).
hacl(user_pod,catalogue_pod, _, _).
hacl(user_pod,kubernetes_dashboard_pod, _, _).
hacl(user_pod,monitoring_influxdb_pod, _, _).
hacl(user_pod,orders_db_pod, _, _).
hacl(user_pod,metrics_server_pod, _, _).
hacl(user_pod,orders_pod, _, _).
hacl(user_pod,catalogue_db_pod, _, _).
hacl(user_pod,user_db_pod, _, _).
hacl(user_pod,internet, _, _).
vulExists(user_pod, 'attacker_can_access_anything', is_computer).
networkServiceInfo(user_pod, is_computer,  _, _ , _).

hacl(monitoring_influxdb_pod,front_end_pod, _, _).
hacl(monitoring_influxdb_pod,rabbitmq_pod, _, _).
hacl(monitoring_influxdb_pod,carts_db_pod, _, _).
hacl(monitoring_influxdb_pod,kubernetes_pod, _, _).
hacl(monitoring_influxdb_pod,heapster_pod, _, _).
hacl(monitoring_influxdb_pod,queue_master_pod, _, _).
hacl(monitoring_influxdb_pod,monitoring_grafana_pod, _, _).
hacl(monitoring_influxdb_pod,kube_dns_pod, _, _).
hacl(monitoring_influxdb_pod,shipping_pod, _, _).
hacl(monitoring_influxdb_pod,payment_pod, _, _).
hacl(monitoring_influxdb_pod,carts_pod, _, _).
hacl(monitoring_influxdb_pod,catalogue_pod, _, _).
hacl(monitoring_influxdb_pod,kubernetes_dashboard_pod, _, _).
hacl(monitoring_influxdb_pod,user_pod, _, _).
hacl(monitoring_influxdb_pod,orders_db_pod, _, _).
hacl(monitoring_influxdb_pod,metrics_server_pod, _, _).
hacl(monitoring_influxdb_pod,orders_pod, _, _).
hacl(monitoring_influxdb_pod,catalogue_db_pod, _, _).
hacl(monitoring_influxdb_pod,user_db_pod, _, _).
hacl(monitoring_influxdb_pod,internet, _, _).
vulExists(monitoring_influxdb_pod, 'attacker_can_access_anything', is_computer).
networkServiceInfo(monitoring_influxdb_pod, is_computer,  _, _ , _).

hacl(orders_db_pod,front_end_pod, _, _).
hacl(orders_db_pod,rabbitmq_pod, _, _).
hacl(orders_db_pod,carts_db_pod, _, _).
hacl(orders_db_pod,kubernetes_pod, _, _).
hacl(orders_db_pod,heapster_pod, _, _).
hacl(orders_db_pod,queue_master_pod, _, _).
hacl(orders_db_pod,monitoring_grafana_pod, _, _).
hacl(orders_db_pod,kube_dns_pod, _, _).
hacl(orders_db_pod,shipping_pod, _, _).
hacl(orders_db_pod,payment_pod, _, _).
hacl(orders_db_pod,carts_pod, _, _).
hacl(orders_db_pod,catalogue_pod, _, _).
hacl(orders_db_pod,kubernetes_dashboard_pod, _, _).
hacl(orders_db_pod,user_pod, _, _).
hacl(orders_db_pod,monitoring_influxdb_pod, _, _).
hacl(orders_db_pod,metrics_server_pod, _, _).
hacl(orders_db_pod,orders_pod, _, _).
hacl(orders_db_pod,catalogue_db_pod, _, _).
hacl(orders_db_pod,user_db_pod, _, _).
hacl(orders_db_pod,internet, _, _).
vulExists(orders_db_pod, 'attacker_can_access_anything', is_computer).
networkServiceInfo(orders_db_pod, is_computer,  _, _ , _).

hacl(metrics_server_pod,front_end_pod, _, _).
hacl(metrics_server_pod,rabbitmq_pod, _, _).
hacl(metrics_server_pod,carts_db_pod, _, _).
hacl(metrics_server_pod,kubernetes_pod, _, _).
hacl(metrics_server_pod,heapster_pod, _, _).
hacl(metrics_server_pod,queue_master_pod, _, _).
hacl(metrics_server_pod,monitoring_grafana_pod, _, _).
hacl(metrics_server_pod,kube_dns_pod, _, _).
hacl(metrics_server_pod,shipping_pod, _, _).
hacl(metrics_server_pod,payment_pod, _, _).
hacl(metrics_server_pod,carts_pod, _, _).
hacl(metrics_server_pod,catalogue_pod, _, _).
hacl(metrics_server_pod,kubernetes_dashboard_pod, _, _).
hacl(metrics_server_pod,user_pod, _, _).
hacl(metrics_server_pod,monitoring_influxdb_pod, _, _).
hacl(metrics_server_pod,orders_db_pod, _, _).
hacl(metrics_server_pod,orders_pod, _, _).
hacl(metrics_server_pod,catalogue_db_pod, _, _).
hacl(metrics_server_pod,user_db_pod, _, _).
hacl(metrics_server_pod,internet, _, _).
vulExists(metrics_server_pod, 'attacker_can_access_anything', is_computer).
networkServiceInfo(metrics_server_pod, is_computer,  _, _ , _).

hacl(orders_pod,front_end_pod, _, _).
hacl(orders_pod,rabbitmq_pod, _, _).
hacl(orders_pod,carts_db_pod, _, _).
hacl(orders_pod,kubernetes_pod, _, _).
hacl(orders_pod,heapster_pod, _, _).
hacl(orders_pod,queue_master_pod, _, _).
hacl(orders_pod,monitoring_grafana_pod, _, _).
hacl(orders_pod,kube_dns_pod, _, _).
hacl(orders_pod,shipping_pod, _, _).
hacl(orders_pod,payment_pod, _, _).
hacl(orders_pod,carts_pod, _, _).
hacl(orders_pod,catalogue_pod, _, _).
hacl(orders_pod,kubernetes_dashboard_pod, _, _).
hacl(orders_pod,user_pod, _, _).
hacl(orders_pod,monitoring_influxdb_pod, _, _).
hacl(orders_pod,orders_db_pod, _, _).
hacl(orders_pod,metrics_server_pod, _, _).
hacl(orders_pod,catalogue_db_pod, _, _).
hacl(orders_pod,user_db_pod, _, _).
hacl(orders_pod,internet, _, _).
vulExists(orders_pod, 'attacker_can_access_anything', is_computer).
networkServiceInfo(orders_pod, is_computer,  _, _ , _).

hacl(catalogue_db_pod,front_end_pod, _, _).
hacl(catalogue_db_pod,rabbitmq_pod, _, _).
hacl(catalogue_db_pod,carts_db_pod, _, _).
hacl(catalogue_db_pod,kubernetes_pod, _, _).
hacl(catalogue_db_pod,heapster_pod, _, _).
hacl(catalogue_db_pod,queue_master_pod, _, _).
hacl(catalogue_db_pod,monitoring_grafana_pod, _, _).
hacl(catalogue_db_pod,kube_dns_pod, _, _).
hacl(catalogue_db_pod,shipping_pod, _, _).
hacl(catalogue_db_pod,payment_pod, _, _).
hacl(catalogue_db_pod,carts_pod, _, _).
hacl(catalogue_db_pod,catalogue_pod, _, _).
hacl(catalogue_db_pod,kubernetes_dashboard_pod, _, _).
hacl(catalogue_db_pod,user_pod, _, _).
hacl(catalogue_db_pod,monitoring_influxdb_pod, _, _).
hacl(catalogue_db_pod,orders_db_pod, _, _).
hacl(catalogue_db_pod,metrics_server_pod, _, _).
hacl(catalogue_db_pod,orders_pod, _, _).
hacl(catalogue_db_pod,user_db_pod, _, _).
hacl(catalogue_db_pod,internet, _, _).
vulExists(catalogue_db_pod, 'attacker_can_access_anything', is_computer).
networkServiceInfo(catalogue_db_pod, is_computer,  _, _ , _).
attackerLocated(user_db_pod).

hacl(user_db_pod,front_end_pod, _, _).
hacl(user_db_pod,rabbitmq_pod, _, _).
hacl(user_db_pod,carts_db_pod, _, _).
hacl(user_db_pod,kubernetes_pod, _, _).
hacl(user_db_pod,heapster_pod, _, _).
hacl(user_db_pod,queue_master_pod, _, _).
hacl(user_db_pod,monitoring_grafana_pod, _, _).
hacl(user_db_pod,kube_dns_pod, _, _).
hacl(user_db_pod,shipping_pod, _, _).
hacl(user_db_pod,payment_pod, _, _).
hacl(user_db_pod,carts_pod, _, _).
hacl(user_db_pod,catalogue_pod, _, _).
hacl(user_db_pod,kubernetes_dashboard_pod, _, _).
hacl(user_db_pod,user_pod, _, _).
hacl(user_db_pod,monitoring_influxdb_pod, _, _).
hacl(user_db_pod,orders_db_pod, _, _).
hacl(user_db_pod,metrics_server_pod, _, _).
hacl(user_db_pod,orders_pod, _, _).
hacl(user_db_pod,catalogue_db_pod, _, _).
hacl(user_db_pod,internet, _, _).
vulExists(user_db_pod, 'attacker_can_access_anything', is_computer).
networkServiceInfo(user_db_pod, is_computer,  _, _ , _).
hacl(kube_dns_pod,internet, UDP, 53).
